# data/raw/

Place the source data files here before running the pipeline:

- `Sales.json` — provided sales transactions (~186 MB, ~298K records)
- `forecast.json` — provided 2009 forecast at Country × Brand grain

These files are gitignored (large size, source data not redistributable in repo).
