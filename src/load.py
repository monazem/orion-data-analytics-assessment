"""load.py — Persist transformed frames as CSV files AND SQLite tables.

Brief says: "Output the final datasets as CSV files OR load them into a
relational database." We do both — CSV satisfies the literal ask and is
easy for Power BI; SQLite demonstrates relational modeling with
proper PK/FK constraints.
"""
# Implementation later in the build.
